 This program depends on a file within the same directory from where it 
gets its inputs. 

It is compiled with following command on terminal:
	g++ -std=c++0x hwk4_frabbi.cpp

After it is complied, it is executed with following command on terminal:
	./a.out input_file